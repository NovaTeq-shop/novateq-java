FROM ubuntu:24.04
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y \
    curl wget jq unzip python3 ca-certificates \
    openjdk-21-jre-headless \
    && rm -rf /var/lib/apt/lists/*
